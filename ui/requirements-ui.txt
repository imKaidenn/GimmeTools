# GimmeTools UI dependencies (separate from the backend).
# The backend runs fine without these — the UI is an optional thin layer.
# GimmeTools.bat installs these automatically on first launch.

pywebview>=5.0          # v2 desktop app (app/)
customtkinter>=5.2.0    # classic fallback UI (ui/gimmetools.py)
